====== 1 =====

[[R760] \IC: 186325650[  ] [PC: 186325362 ] [ST: 1MK4B04 ]]
TS: yao Tina| IM:N/A |SAM:N/A |SST: 186386838 Guoxun

[OS: ] [Kernel: ] [OEM: Y]

Exce-All is ok|vmware|NSX issue

--- 描述 ---

Escalation Reason: Others;

Product Type: 服务器;

Product Model: R760;

Others;

Detail Symptom Descriptions(故障现象):

esxi order：714514640

客户咨询

1. 今日在做VPC的测试，手动创建的自动分配地址池子网在关联VM后无法获取到地址，但是系统生成的AVI_SUBNET--LB就可以，能否予以支持？

2. 每个VPC在创建后自动生成了一个T1-gateway，我在一个租户项目下，创建了2个VPC，能否实现该租户下的不能VPC子网间的通信？

[    ]（该场景在于同一个租户，一个VPC用于生产环境，一个VPC用于调试环境，调试环境可能需要访问生产环境的数据以验证功能。）

;

Troubleshooting Steps(详细诊断步骤):

esxi order：714514640

客户咨询

1. 今日在做VPC的测试，手动创建的自动分配地址池子网在关联VM后无法获取到地址，但是系统生成的AVI_SUBNET--LB就可以，能否予以支持？

2. 每个VPC在创建后自动生成了一个T1-gateway，我在一个租户项目下，创建了2个VPC，能否实现该租户下的不能VPC子网间的通信？

[    ]（该场景在于同一个租户，一个VPC用于生产环境，一个VPC用于调试环境，调试环境可能需要访问生产环境的数据以验证功能。）

;

Current status(当前状态和要求): 帮忙看下是否可以支持;

====== 2 =====

This issue has been resolved and new issues are being followed up in new IC.

---------------------\
Waiting for a response from the customer as to whether[  resolved.\
]------------------------------\
Waiting for further feedback from customers.\
------------------------------\
Waiting for customer to have testing finished.

----------------------------\
The problem is under further resolution.\
----------------------------\
Analyzing:

The customer was doing VPC testing. The manually created automatically assigned address pool subnet could not obtain an address after being associated with the VM, but the AVI_SUBNET--LB generated by the system could.

Solution:

Suggest provide more details information.

Next step:

Suggest provide more details information.

====== 3 =====

Resolution：

 

Root Cause: 

 

 

----Remark----

 

 

Guoxun：

1. 今日在做VPC的测试，手动创建的自动分配地址池子网在关联VM后无法获取到地址，但是系统生成的AVI_SUBNET--LB就可以，能否予以支持？

2. 每个VPC在创建后自动生成了一个T1-gateway，我在一个租户项目下，创建了2个VPC，能否实现该租户下的不能VPC子网间的通信？

   （该场景在于同一个租户，一个VPC用于生产环境，一个VPC用于调试环境，调试环境可能需要访问生产环境的数据以验证功能。）

3. 针对租户要使用ALB功能实现服务发布的情况，我在创建vip时看不到租户里面单独为SE创建的管理和数据分段，也需要你们的帮助。

他提了三个问题，第一个和第二个可以合成一个

第三个我远程的时候看看，如果没能直接解决，可能还要单独开个CASE
