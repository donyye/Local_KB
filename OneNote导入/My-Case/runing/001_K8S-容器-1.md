K8S-容器-1

1. 安装 Ubuntu 系统 ：
2. 安装 docker ：
3. 在 docker 运行容器 ：

4. 进入容器的方法：

-------------------------------------------------------------------------

1. 安装 Ubuntu 系统

 

2. 安装 docker 

国外：

<https://docs.docker.com/engine/install/ubuntu/>

\
国内：
[https://developer.aliyun.com/mirror/](https://developer.aliyun.com/mirror/)

选择 --\> docker-ce , 然后你可以看到不同的系统不同的安装方法

一般使用国内的镜像，比较快。

 

[ ------ ubuntu ]安装方法 ------

# step 1: 安装必要的一些系统工具

sudo apt-get update

sudo apt-get install ca-certificates curl gnupg

 

# step 2: 信任 Docker 的 GPG 公钥

sudo install -m 0755 -d /etc/apt/keyrings

curl -fsSL <https://mirrors.aliyun.com/docker-ce/linux/ubuntu/gpg> | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

sudo chmod a+r /etc/apt/keyrings/docker.gpg

 

# Step 3: 写入软件源信息

echo \

  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] <https://mirrors.aliyun.com/docker-ce/linux/ubuntu> \

  "$(. /etc/os-release && echo "$VERSION_CODENAME")" stable" | \

  sudo tee /etc/apt/sources.list.d/docker.list \> /dev/null

 

# Step 4: 安装Docker

sudo apt-get update

sudo apt-get install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

 

# 安装指定版本的Docker-CE:

# Step 1: 查找Docker-CE的版本:

# apt-cache madison docker-ce

#   docker-ce | 17.03.1~ce-0~ubuntu-xenial | [https://mirrors.aliyun.com/docker-ce/linux/ubuntu xenial/stable](https://mirrors.aliyun.com/docker-ce/linux/ubuntu%20xenial/stable) amd64 Packages

#   docker-ce | 17.03.0~ce-0~ubuntu-xenial | [https://mirrors.aliyun.com/docker-ce/linux/ubuntu xenial/stable](https://mirrors.aliyun.com/docker-ce/linux/ubuntu%20xenial/stable) amd64 Packages

# Step 2: 安装指定版本的Docker-CE: (VERSION例如上面的17.03.1~ce-0~ubuntu-xenial)

# sudo apt-get -y install docker-ce=[VERSION]

 ------ done------

 

配置一个镜像加速器：

使用阿里云个人镜像仓库，需要有账号。

<https://cr.console.aliyun.com/cn-hangzhou/instances/mirrors>

主要是为了做加速

sudo mkdir -p /etc/docker 

 

sudo tee /etc/docker/daemon.json \<\<-'EOF' 

{

[ "registry-mirrors": \"[https://9v94unqh.mirror.aliyuncs.com](https://9v94unqh.mirror.aliyuncs.com)["] ]

} 

EOF 

 

$ sudo systemctl daemon-reload 

$ sudo systemctl restart docker

$ sudo systemctl enable --now docker

$ sudo systemctl status docker

 

或者使用下面多个加速器

-----------------------------------------

-----------------------------------------

-----------------------------------------

 

3. 在 docker 运行容器 

切换到root用户(因为docker 需要此用运行)
$ sudo -i

 

# docker run -p 8080:80 -d httpd

Unable to find image 'httpd:latest' locally

latest: Pulling from library/httpd

8a628cdd7ccc: Pull complete

60ba3d18ad64: Pull complete

4f4fb700ef54: Pull complete

03e322382f93: Pull complete

4ad6b63c403f: Pull complete

c613327bbca6: Pull complete

Digest: sha256:4564ca7604957765bd2598e14134a1c6812067f0daddd7dc5a484431dd03832b

Status: Downloaded newer image for httpd:latest

0ae859886f8024058e249965361259cc6550fd740cc0651b72d5e5ca32cd1876

 

查看本地镜像

# docker images

REPOSITORY   TAG       IMAGE ID       CREATED        SIZE

httpd        latest    10fd72f437c4   3 months ago   148MB

 

查看正在运行的容器

# docker ps

CONTAINER ID   IMAGE     COMMAND              CREATED        STATUS        PORTS                                     NAMES

0ae859886f80   httpd     "httpd-foreground"   21 hours ago   Up 21 hours   0.0.0.0:8080-\>80/tcp, [::]:8080-\>80/tcp   serene_shtern

 

验证容器可以使用8080访问

# curl localhost:8080

\<html\>\<body\>\<h1\>It works!\</h1\>\</body\>\</html\>

 

停止容器

# docker stop serene_shtern

serene_shtern

# tab 键补齐后 serene_shtern 就是在运行容器的名字

 

查看所有已停止的容器

# docker ps -a

CONTAINER ID   IMAGE     COMMAND              CREATED        STATUS                          PORTS     NAMES

0ae859886f80   httpd     "httpd-foreground"   21 hours ago   Exited (0) About a minute ago             serene_shtern

 

重新启动容器

# docker start serene_shtern 

serene_shtern

 

 

4. 进入容器的方法：

1. docker attach[  --\> ]直接进入已启动容器的命令终端，不会启动新的进程。

2. docker exec --\> 在容器打开新的终端

 

# docker run -d centos

Unable to find image 'centos:latest' locally

latest: Pulling from library/centos

a1d0c7532777: Pull complete

Digest: sha256:a27fd8080b517143cbbbab9dfb7c8571c40d67d534bbdee55bd6c473f432b177

Status: Downloaded newer image for centos:latest

89a2ee957402cf3fe395ddecf377d19cf0f1923248542c9d7bfc7ca0ceded46f

 

# docker run -it -d centos /bin/bash -c "while true; do sleep 1; echo haha; done"

44a8fb0e97c2e69f70e1f0f91d965f7886be82315c8075b7017c9dbf2dd5ede2

# 如果不加 -it, attach 到容器里会无法离开。使用 \<CTRL -p\> \<CTRL -q\> 退出

 

# docker ps

CONTAINER ID   IMAGE     COMMAND                  CREATED              STATUS              PORTS     NAMES

44a8fb0e97c2   centos    "/bin/bash -c 'while..."   About a minute ago   Up About a minute             eloquent_keller

 

使用 docker attach[  ]进入到容器查看

# docker attach elastic_rubin 

haha

haha

haha

haha

haha

haha

haha

read escape sequence [ \<-- ]使用 \<CTRL -p\> \<CTRL -q\> 退出

 

使用 docker exec 进入到容器交互

# docker exec -it elastic_rubin bash

[root@d8d472edcd6b /]# pwd

/

[root@d8d472edcd6b /]# ls

bin  dev  etc  home  lib  lib64  lost+found  media  mnt  opt  proc  root  run  sbin  srv  sys  tmp  usr  var

[root@d8d472edcd6b /]#    

[root@d8d472edcd6b /]# cat /etc/redhat-release

CentOS Linux release 8.4.2105

[root@d8d472edcd6b /]#

[root@d8d472edcd6b /]# exit

exit

 

 

补充两个命令，批量stop容器和批量删除容器

# docker stop $(docker ps -q)

# docker rm $(docker ps -a -q)
