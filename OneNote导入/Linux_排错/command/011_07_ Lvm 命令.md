07: Lvm 命令

2023年9月11日

12:04

1. lvm vgchange -ay

》LVM 会尝试激活系统中所有处于未激活状态的卷组。这将导致系统上的逻辑卷变为可用状态，可以被挂载并用于存储数据。

vgchange: 这部分命令指示 LVM 工具执行卷组的更改操作。

-ay: 这是选项的组合，具有以下含义：

-a: 激活选项。它告诉 LVM 激活卷组，使其内部的逻辑卷变得可用。

-y: 在执行命令时不要提示确认。通常情况下，LVM 会要求用户确认激活操作，但使用 -y 选项可以跳过确认步骤，直接执行激活操作。

 

pvcreate /dev/sdb\
[ ]

 

 

已使用 OneNote 创建。
