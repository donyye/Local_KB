= 环境准备 =v9.0

2024年12月24日

15:04

rht-vmctl start classroom[  ]（先让它运行一下）

rht-vmctl start all

 

greg 用户[  flectrag]

\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\-\--

\$ cd ansible

\$ bash setup.sh[   ]运行脚步，做初始化配置，没有报错后才能做题。

 

\$ ansible all -m shell -a "vgscan"[   \--]》查看是否有卷组

===================

关于RHCE中，如何使用doc快速查询

命令格式

ansible-navigator doc 模块名 -m stdout

以yum为例

ansible-navigator doc yum -m stdout

随后输入/EXAMPLES

定位到使用例子

 

1、CE考试全程是控制节点操作其他节点，模拟环境使用的用户是greg（切记不要进root），所有系统的密码是 flectrag

2、如记不住可以多用帮助文档查询：ansible-navigator doc 模块名（例如：ansiblenavigator doc setup \#模块）

 

已使用 OneNote 创建。
