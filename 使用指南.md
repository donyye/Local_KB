# 快速上手（存给自己看）

## 1. 打开库
装好 Obsidian → `Open folder as vault` → 选择 `E:\Local_KB`

## 2. 一次性设置（让截图自动归位）
设置 → `Files & Links` → `Default location for new attachments`
→ 选 **In the folder specified below** → 填 `attachments`

## 3. 写第一篇
新建笔记 → 粘贴截图 → 写完就完事，不用想结构。

## 4. 串起来
攒几篇同主题后，在 `索引.md` 加一行 `[[笔记名]]`。

## 以后（别现在折腾）
- 标签 `#`、反向链接、图谱视图
- PARA 分层、Dataview / Templater 插件

## 5. 换机器 / 多设备同步（GitHub 私有仓库）

仓库地址：`https://github.com/donyye/Local_KB.git`（分支 `main`，私有）

### 在新机器上拉取
1. 装 **Obsidian** + **Git**（git-scm.com，安装时勾选 Git from command line）
2. 终端运行：`git clone https://github.com/donyye/Local_KB.git`
3. 认证：用户名 `donyye`，**密码填 PAT**（`ghp_` 开头那串，不是 GitHub 登录密码）
4. 记住凭据（免重复输入）：`git config --global credential.helper wincred`
5. Obsidian → Open folder as vault → 选 clone 出来的 `Local_KB` 文件夹
6. 装 **Obsidian Git** 插件（作者 Vinzent03）→ 开启：
   - Auto pull on startup
   - Auto backup interval（如 10 分钟，定时 commit + push）
   - Automatically push after backup

### 日常使用
- 任一台编辑 → 插件定时自动 push 到 GitHub
- 另一台 → 插件自动 pull（或手动点「同步」）拿最新
- 新机器首次打开先点一次 **Pull**

### 注意事项
- 别两台同时改同一文件，会冲突需手动合并
- 报 `filename too long`：`git config --global core.longpaths true`
- 首次 clone 慢（4808 张图 / ~500MB）正常
- 网页 GitHub 不渲染 `![[...]]`，Obsidian 里一切正常
- PAT 安全：建议每台机器用各自重新生成的 PAT，或配 SSH key
